#include "main.h"
/**
 *_strlen_recursion - returns the length of a string
 *@s: the string to count
 *Return: integer.
 */


int _strlen_recursion(char *s)
{
	if (s[n] != '\0')
	{
		n++	
		return (0);
	}
	
	_strlen_recursion(s);	
	return(n);
}
